# Block lifecycle scripts during install (preinstall/postinstall worms). Run `npm run apply-patches` after install.
ignore-scripts=true

# Reject installs that do not match package-lock.json when using `npm ci`.
package-lock=true

# Surface dependency issues early.
audit-level=moderate
