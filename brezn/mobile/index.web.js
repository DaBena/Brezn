import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('Brezn', () => App);
AppRegistry.runApplication('Brezn', {
  rootTag: document.getElementById('root')
});
